const classes = {
  button: 'btn',
  link: 'btn-link',
  primary: 'btn-primary',
  secondary: 'btn-secondary',
};

const options = {};

const styles = {};

export default { classes, options, styles };
