const classes = {
  tab: 'nav-link',
  tabContent: 'tab-content pad-y',
  tabList: 'nav nav-tabs',
  tabPane: 'tab-pane',
};

const options = {};

const styles = { tab: { cursor: 'pointer', outline: 'none' } };

export default { classes, options, styles };
