const classes = {
  modal: 'modal fade in',
  dialog: 'modal-dialog',
  dialogBody: 'modal-body',
  dialogContent: 'modal-content',
  dialogHeader: 'handle modal-header',
  dialogTitle: 'handle modal-title',
};

const options = {};

const styles = {};

export default { classes, options, styles };
